<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation"
    app:startDestination="@id/popularFragment">

    <fragment
        android:id="@+id/popularFragment"
        android:name="com.pinygod.exchangeratesapp.presentation.rates.RatesFragment"
        android:label="PopularFragment" >
        <argument
            android:name="isFavorite"
            app:argType="boolean"
            android:defaultValue="false" />
    </fragment>
    <fragment
        android:id="@+id/favoriteFragment"
        android:name="com.pinygod.exchangeratesapp.presentation.rates.RatesFragment"
        android:label="PopularFragment" >
        <argument
            android:name="isFavorite"
            app:argType="boolean"
            android:defaultValue="true" />
    </fragment>
    <!--<fragment
        android:id="@+id/favoriteFragment"
        android:name="com.pinygod.exchangeratesapp.presentation.rates.FavoriteFragment"
        android:label="FavoriteFragment" >
        <argument
            android:name="isFavorite"
            app:argType="boolean"
            android:defaultValue="true" />
    </fragment>-->
</navigation>